<h1>Booking Done</h1>

<h2>Flight information</h2>
<table>
	<thead>
		<tr>
			<td>Departure</td>
			<td>Arrival</td>
			<td>Date</td>
			<td>Time</td>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td><%= @booking.flight.departure_airport.name %></td>
			<td><%= @booking.flight.arrival_airport.name %></td>
			<td><%= @booking.flight.date.strftime("%a, %b %d  %Y") %></td>
			<td><%= @booking.flight.date.strftime("%H:%M") %></td>
		</tr>
	</tbody>
</table>
<h2><%= "Passenger".pluralize(@booking.passengers_number) %> information</h2>
<table>
	<thead>
		<tr>
			<td>Name</td>
			<td>email</td>
		</tr>
	</thead>
	<tbody>
		<% @booking.passengers.each do |passenger| %>
			<tr>
				<td><%= passenger.name %></td>
				<td><%= passenger.email %></td>
			</tr>
		<% end %>	
	</tbody>
</table>